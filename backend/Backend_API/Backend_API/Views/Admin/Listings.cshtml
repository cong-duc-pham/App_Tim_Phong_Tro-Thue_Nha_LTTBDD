@model Backend_API.Models.ViewModels.Admin.AdminListingsViewModel
@{
    ViewData["Title"] = "Duyá»‡t Tin ÄÄƒng";
}

@if (TempData["AdminSuccess"] is string successMessage)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @successMessage
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

@if (TempData["AdminError"] is string errorMessage)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        @errorMessage
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}
<div class="card border-0 shadow-sm">
    <div class="card-header bg-white fw-semibold">Danh sÃ¡ch pending</div>
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th>#ID</th>
                        <th>TiÃªu Ä‘á»</th>
                        <th>GiÃ¡</th>
                        <th>Chá»§ tin</th>
                        <th>NgÃ y táº¡o</th>
                        <th class="text-end">Action</th>
                    </tr>
                </thead>
                <tbody>
                    @if (!Model.Listings.Any())
                    {
                        <tr>
                            <td colspan="6" class="text-center py-4 text-muted">KhÃ´ng cÃ³ tin pending.</td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var listing in Model.Listings)
                        {
                            <tr>
                                <td>@listing.ListingId</td>
                                <td>@listing.Title</td>
                                <td>@listing.Price.ToString("N0") Ä‘</td>
                                <td>
                                    <div>@listing.LandlordName</div>
                                    <small class="text-muted">@(listing.LandlordEmail ?? "-")</small>
                                </td>
                                <td>@(listing.CreatedAt?.ToLocalTime().ToString("dd/MM/yyyy HH:mm") ?? "-")</td>
                                <td class="text-end">
                                    <div class="d-inline-flex gap-2">
                                        <form method="post" action="/admin/listings/@listing.ListingId/approve">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-outline-success">Approve</button>
                                        </form>
                                        <form method="post" action="/admin/listings/@listing.ListingId/reject" class="d-flex gap-2">
                                            @Html.AntiForgeryToken()
                                            <input type="text" name="reason" class="form-control form-control-sm" placeholder="LÃ½ do tá»« chá»‘i" />
                                            <button type="submit" class="btn btn-sm btn-outline-danger">Reject</button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
