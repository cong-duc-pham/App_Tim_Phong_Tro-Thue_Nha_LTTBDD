@model Backend_API.Models.ViewModels.Admin.AdminUsersViewModel
@{
    ViewData["Title"] = "Quản lý Users";
}

@if (TempData["AdminSuccess"] is string successMessage)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @successMessage
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

@if (TempData["AdminError"] is string errorMessage)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        @errorMessage
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<div class="card border-0 shadow-sm">
    <div class="card-header bg-white d-flex flex-wrap align-items-center justify-content-between gap-2">
        <div>
            <div class="fw-semibold">Danh s&aacute;ch Users</div>
            <div class="small text-muted">Bấm đồng bộ để lấy user từ Firebase Auth về SQL.</div>
        </div>
        <form method="post" action="/admin/users/sync-firebase" class="m-0">
            @Html.AntiForgeryToken()
            <button type="submit" class="btn btn-sm btn-primary">Đồng bộ Firebase users</button>
        </form>
    </div>
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th>Name</th>
                        <th>Email</th>
                        <th>Role</th>
                        <th>firebase_uid</th>
                        <th>auth_method</th>
                        <th>Trạng th&aacute;i</th>
                        <th class="text-end">Action</th>
                    </tr>
                </thead>
                <tbody>
                    @if (!Model.Users.Any())
                    {
                        <tr>
                            <td colspan="7" class="text-center py-4 text-muted">Kh&ocirc;ng c&oacute; dữ liệu.</td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var user in Model.Users)
                        {
                            <tr>
                                <td>@user.FullName</td>
                                <td>@(string.IsNullOrWhiteSpace(user.Email) ? "-" : user.Email)</td>
                                <td>@user.RoleName</td>
                                <td>
                                    @if (string.IsNullOrWhiteSpace(user.FirebaseUid))
                                    {
                                        <span class="badge bg-secondary">Kh&ocirc;ng</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-success">C&oacute;</span>
                                    }
                                </td>
                                <td>@user.AuthMethod</td>
                                <td>
                                    @if (user.IsActive)
                                    {
                                        <span class="badge bg-success">Đang hoạt động</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger">Đ&atilde; kh&oacute;a</span>
                                    }
                                </td>
                                <td class="text-end">
                                    <form method="post" action="/admin/users/@user.UserId/toggle-active" class="d-inline">
                                        @Html.AntiForgeryToken()
                                        @if (user.IsActive)
                                        {
                                            <button type="submit" class="btn btn-sm btn-outline-danger">Kh&oacute;a</button>
                                        }
                                        else
                                        {
                                            <button type="submit" class="btn btn-sm btn-outline-success">Mở kh&oacute;a</button>
                                        }
                                    </form>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
