@model Backend_API.Models.ViewModels.Admin.AdminViewingAppointmentsViewModel
@{
    ViewData["Title"] = "Theo dõi lịch xem phòng";
    var statuses = new[]
    {
        ("", "Tất cả"),
        ("pending", "Đang chờ"),
        ("confirmed", "Đã xác nhận"),
        ("declined", "Từ chối"),
        ("cancelled", "Đã hủy")
    };
    string StatusLabel(string status) => status switch
    {
        "confirmed" => "Đã xác nhận",
        "declined" => "Từ chối",
        "cancelled" => "Đã hủy",
        _ => "Đang chờ"
    };
    string StatusClass(string status) => status switch
    {
        "confirmed" => "text-bg-success",
        "declined" => "text-bg-danger",
        "cancelled" => "text-bg-secondary",
        _ => "text-bg-warning"
    };
}

<div class="d-flex flex-wrap align-items-center justify-content-between gap-3 mb-3">
    <div>
        <h2 class="h5 mb-1">Theo dõi lịch xem phòng</h2>
        <p class="text-muted mb-0">Admin chỉ theo dõi; chủ trọ là người xác nhận hoặc từ chối yêu cầu.</p>
    </div>
    <span class="badge text-bg-primary rounded-pill">@Model.Appointments.Count lịch</span>
</div>

<div class="card border-0 shadow-sm">
    <div class="card-body border-bottom">
        <form method="get" class="d-flex flex-wrap gap-2 align-items-center">
            <label for="status" class="fw-semibold">Trạng thái</label>
            <select id="status" name="status" class="form-select" style="max-width: 210px">
                @foreach (var option in statuses)
                {
                    if (Model.Status == option.Item1)
                    {
                        <option value="@option.Item1" selected>@option.Item2</option>
                    }
                    else
                    {
                        <option value="@option.Item1">@option.Item2</option>
                    }
                }
            </select>
            <button type="submit" class="btn btn-primary">Lọc</button>
            <a href="/admin/viewing-appointments" class="btn btn-outline-secondary">Xóa lọc</a>
        </form>
    </div>
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th>#</th>
                        <th>Phòng</th>
                        <th>Người thuê</th>
                        <th>Chủ trọ</th>
                        <th>Thời gian xem</th>
                        <th>Trạng thái</th>
                        <th>Ghi chú</th>
                    </tr>
                </thead>
                <tbody>
                    @if (!Model.Appointments.Any())
                    {
                        <tr>
                            <td colspan="7" class="text-center py-5 text-muted">Chưa có lịch xem phòng phù hợp.</td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var item in Model.Appointments)
                        {
                            <tr>
                                <td>@item.AppointmentId</td>
                                <td>
                                    <a href="/admin/listings/@item.ListingId?from=management" class="fw-semibold text-decoration-none">@item.ListingTitle</a>
                                    <div class="small text-muted">Tin #@item.ListingId</div>
                                </td>
                                <td>
                                    <div>@item.TenantName</div>
                                    <div class="small text-muted">@(item.TenantPhone ?? "-")</div>
                                </td>
                                <td>
                                    <div>@item.LandlordName</div>
                                    <div class="small text-muted">@(item.LandlordPhone ?? "-")</div>
                                </td>
                                <td>@item.ScheduledAt.ToLocalTime().ToString("dd/MM/yyyy HH:mm")</td>
                                <td><span class="badge @StatusClass(item.Status)">@StatusLabel(item.Status)</span></td>
                                <td style="min-width: 220px">
                                    @if (!string.IsNullOrWhiteSpace(item.TenantNote))
                                    {
                                        <div><span class="small text-muted">Khách:</span> @item.TenantNote</div>
                                    }
                                    @if (!string.IsNullOrWhiteSpace(item.LandlordNote))
                                    {
                                        <div><span class="small text-muted">Chủ trọ:</span> @item.LandlordNote</div>
                                    }
                                    @if (string.IsNullOrWhiteSpace(item.TenantNote) && string.IsNullOrWhiteSpace(item.LandlordNote))
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
