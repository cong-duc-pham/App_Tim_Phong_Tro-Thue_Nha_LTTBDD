rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The application stores business data in SQL Server.
    // No Firestore collection is required, so access is denied by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
